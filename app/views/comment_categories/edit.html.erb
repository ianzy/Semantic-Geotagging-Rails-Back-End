<h1>Editing comment_category</h1>

<% form_for(@comment_category) do |f| %>
  <%= f.error_messages %>

  <p>
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </p>
  <p>
    <%= f.submit 'Update' %>
  </p>
<% end %>

<%= link_to 'Show', @comment_category %> |
<%= link_to 'Back', comment_categories_path %>